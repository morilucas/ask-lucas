# Production content boundary

Production biography is not stored in this public repository. The API receives a reviewed Markdown directory from a separate private repository through `ASK_LUCAS_CONTENT_DIR`.

Synthetic files for local development and tests live under `examples/content/`.

# Plan 005: Premium chat interface

1. Replace the marketing-page hierarchy with a viewport-filling chat application shell.
2. Restyle the empty state, transcript, source treatment, composer, and inspector using existing
   platform primitives and API behavior.
3. Update focused browser expectations for the new semantic layout while preserving behavioral
   coverage.
4. Run static, build, interaction, responsive, and visual checks.
5. Publish and deploy the verified interface to `ask.lkmori.com`.


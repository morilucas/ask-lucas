# Plan 006: dark-first adaptive chat theme

1. Add semantic dark-default tokens and complete light-theme overrides.
2. Apply a saved theme before hydration and add a small client-side header toggle.
3. Restyle message roles, chrome, composer, cards, evidence, and system surfaces using the tokens.
4. Extend browser coverage for default, switching, persistence, and mobile overflow.
5. Verify both appearances, record the visual decision, and deploy only the web service.

